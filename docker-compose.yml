services:
  # Database: Vector DB for FAQ RAG
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_ecommerce
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    restart: unless-stopped

  # # Backend: FastAPI AI Agent
  # backend:
  #   image: bhaveshnegi/ecommerce-ai-backend:1.0.1
  #   container_name: agent_backend
  #   ports:
  #     - "8000:8000"
  #   env_file: .env
  #   depends_on:
  #     - qdrant
  #   volumes:
  #     - ./db:/app/db
  #   restart: unless-stopped

  # # Frontend: Nginx Static Interface
  # frontend:
  #   image: bhaveshnegi/ecommerce-ai-frontend:1.0.1
  #   container_name: agent_frontend
  #   ports:
  #     - "5000:80"
  #   depends_on:
  #     - backend
  #   restart: unless-stopped
